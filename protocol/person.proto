// 指定使用 proto3 语法
syntax = "proto3";

// 为生成的 C++ 代码指定命名空间
package tutorial;

// 定义一个 "Person" 消息类型
message Person {
  string name = 1;         // 姓名
  int32 id = 2;            // ID
  repeated string emails = 3; // 邮箱地址（可以有多个）
}